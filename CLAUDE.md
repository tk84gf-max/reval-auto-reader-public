# ReVal 自動読み取り — 作業方針

> **初めて使う方へ**: これは改造する人向けの文書です。まず使えるようにしたい場合は
> [はじめに読む_ClaudeCodeへの指示.md](はじめに読む_ClaudeCodeへの指示.md) と `docs/` を読んでください。

マイソク（販売図面）から不動産の収益指標を算出するローカルアプリ。
Node標準ライブラリのみ・npm依存なし・ビルド工程なし。全体で約2,100行。

## 役割分担

メインセッションは **判断・設計・ドメインルールの決定・品質ゲート** に徹する。
以下は専用エージェントへ委任する（`.claude/agents/`）:

| 委任先 | 用途 |
|---|---|
| `engine-parity` | 計算ロジックの重複箇所の突き合わせ（**計算式を触ったら必ず実行**） |
| `web-researcher` | 税制・法定耐用年数・融資慣行など外部情報の調査 |
| `ui-mechanic` | 一括置換・ラベル文言の統一・整形 |

実装そのものはメインに残す。このリポジトリの実装は不動産ドメインの判断と
不可分で、切り出すと要件伝達コストのほうが高くつくため。

## 計算ロジックの重複（最重要）

**同じ計算式が複数ファイルに別実装で存在する。片方だけ直すと静かにズレる。**

- 法定耐用年数テーブル … `ReVal.html` の `legalLife()` /
  `scripts/reval_calc.js` の `ll` / `scripts/prefill_reval.js` の `_ll` の**3箇所**
- 融資期間の自動算出（残存<10年→10年、それ以外 min(残存,30)）…
  `scripts/reval_calc.js` の `autoLoanTerm()` / `scripts/prefill_reval.js` に**インラインで再実装**。
  `ReVal.html` には自動算出が無く、手入力（初期値26年）
- 5軸グレード閾値 … `ReVal.html` の `gradeCCR/gradeRepay/gradeCFR`

**計算式・定数を変更したら、必ず `engine-parity` で全箇所を照合すること。**

## ドメイン不変条件（勝手に変えない）

- 有効率デフォルトは全構造80%（作者指示 2026-08-22。木造100%のIG準拠案は廃止）
- 法定耐用年数：木造22 / 軽量鉄骨3mm以下19 / 軽量鉄骨3〜4mm27 / 重量鉄骨4mm超34 / RC・SRC47
- `ReVal.html` は**1ファイル完結**が要件。外部JS/CSSへ分割しない
  （ユーザーがメモ帳で開いて費率を書き換える運用のため）
- APIキーは `~/.config/reval/apikey.txt` 固定。リポジトリ内に置かない・書き出さない
- 上記数値の変更は、作者の明示指示がある時のみ

## やらないこと

- 一括置換やフォーマット統一をメインモデルにやらせない → `ui-mechanic` へ
- 調べものをメインモデルにやらせない → `web-researcher` へ
- **2回直して直らないものを3回目でやらせない。**
  同じ箇所を繰り返し直している時点でやり方が間違っている。モデルを上げても直らない。
  手を止めて設計から見直す
