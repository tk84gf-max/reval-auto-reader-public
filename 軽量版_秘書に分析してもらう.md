# 軽量版 — Claude Code（AI秘書）に分析してもらう使い方

アプリを起動せず、**Claude Code のチャットにマイソクを渡して分析してもらう**やり方です。
セットアップはほぼありません。Anthropic の APIキーも要りません（Claude Code の契約でそのまま動きます）。

## 必要なもの

- **Claude Code**（すでにお使いの環境でOK）
- **Node.js**（計算エンジンの実行に使います。`node -v` でバージョンが出ればOK）

APIキーが要るのは、別冊の「アプリ版」（マイソクをブラウザにドロップする方式）だけです。こちらでは使いません。

## 使うファイル

| ファイル | 役割 |
|---|---|
| `scripts/reval_calc.js` | 計算エンジン。指標をすべて算出し、評価カルテを出す |
| `scripts/prefill_reval.js` | 物件専用の ReVal HTML を生成する |
| `ReVal.html` | 計算エンジン本体（テンプレート兼、手入力の電卓） |
| `docs/入力項目リファレンス.md` | 読み取る項目とJSONの形、標準値、計算式 |

## 手順

1. このフォルダで Claude Code を開く
2. マイソク（PDF・画像）をチャットに渡す
3. 下の指示文を貼る

## Claude Code に貼る指示文

```
このフォルダにある ReVal の計算エンジンを使って、私が渡すマイソク（販売図面）を分析してください。

【手順】
1. マイソクから次の項目を読み取ってください。
   必須: 物件名 / 住所 / 価格(万円) / 表面利回り(%)（無ければ 満室年収÷価格 で計算） /
         延床面積(㎡) / 構造 / 築年数 / 戸数
   任意: 土地面積(㎡) / 路線価(万円/㎡) / レントロールの満室年収・現況年収と実費
2. 読み取った値を一覧で見せて、私の確認を取ってください。自信のない項目は「不明」と書いてください。
3. 確認が取れたら docs/入力項目リファレンス.md の形式で input.json を作り、
   node scripts/reval_calc.js input.json
   を実行して評価カルテを出してください。
4. 続けて
   node scripts/prefill_reval.js input.json
   を実行し、生成された analysis/ の中の HTML のパスを教えてください。
5. カルテの要点（年間CF・CCR・返済比率・CF率・積算比率・税引後CFと各格付け）を日本語でまとめてください。

【守ってほしいこと】
- 融資・費用の条件は標準セット（LTV90% / 金利2.0% / 諸費用7% / 管理5.5% / 有効率80% / 空室5%）を
  そのまま使ってください。私が指定したときだけ変えてください。
- 資料に無い数字を推測で埋めないでください。特に価格・利回り・面積・築年・構造。
- 路線価が資料に無ければ、積算の土地部分は「未算定」としてください。公示地価などから推定しないでください。
- 数字の根拠がマイソクのどこにあったかを、一言添えてください。
```

## 出てくるもの

- **評価カルテ**（チャット上のテキスト）… 収入・支出・NOI・返済・CF・CCR・返済比率・CF率・積算・税引後CFと A〜D の格付け
- **物件専用の HTML**（`analysis/` の中）… ブラウザで開くと、数字を動かしながら再計算できます。価格を動かせば指値の検討に、期間とLTVを変えれば融資条件の比較に使えます

## 補足

- 何も渡さずに `ReVal.html` をダブルクリックすれば、手入力の電卓としても使えます（Node も Claude Code も不要）
- 対象は住居系の一棟収益物件です。商業・テナント・借地権は注意表示は出ますが、数値は目安です
- 本ツールは投資判断の補助です。最終判断はご自身の責任でお願いします
